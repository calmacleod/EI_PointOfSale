<% content_for :title, "Edit supplier" %>
<div class="w-full">
  <%= render "shared/page_header", title: "Edit supplier", description: "Update #{@supplier.name}." %>

  <div class="mt-2 form-card overflow-hidden rounded-lg border border-theme bg-surface">
    <%= render "form", supplier: @supplier %>
  </div>
</div>
