<% content_for :title, "New supplier" %>
<div class="w-full">
  <%= render "shared/page_header", title: "New supplier", description: "Add a supplier for product sourcing." %>

  <div class="mt-2 form-card overflow-hidden rounded-lg border border-theme bg-surface">
    <%= render "form", supplier: @supplier %>
  </div>
</div>
