<% content_for :title, "Edit #{@receipt_template.name}" %>

<div class="w-full">
  <%= render "shared/page_header", title: "Edit Receipt Template" %>

  <div class="mt-3">
    <%= render "form", receipt_template: @receipt_template, store: @store %>
  </div>
</div>
