<% content_for :title, "New product" %>
<div class="w-full">
  <%= render "shared/page_header", title: "New product", description: "Create a product with code, pricing, and stock details." %>

  <div class="ui-card form-card mt-2">
    <%= render "form", product: @product %>
  </div>
</div>
