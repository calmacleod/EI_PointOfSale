<% content_for :title, "New service" %>
<div class="w-full">
  <%= render "shared/page_header", title: "New service", description: "Create a sellable service." %>

  <div class="ui-card form-card mt-2">
    <%= render "form", service: @service %>
  </div>
</div>
