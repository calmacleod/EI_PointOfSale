<% content_for :title, "Edit #{@customer.name}" %>
<div class="w-full">
  <%= render "shared/page_header", title: "Edit #{@customer.name}", description: "Update customer details." %>

  <div class="mt-2 form-card overflow-hidden rounded-lg border border-theme bg-surface">
    <%= render "form", customer: @customer %>
  </div>
</div>
