<% content_for :title, "Edit profile" %>
<div class="w-full">
  <%= render "shared/page_header", title: "Your profile", description: "Update your personal details and password." %>

  <div id="profile_form" class="mt-2 overflow-hidden rounded-lg border border-theme bg-surface">
    <%= render "form", user: @user %>
  </div>
</div>
