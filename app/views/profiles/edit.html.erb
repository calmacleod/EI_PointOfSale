<% content_for :breadcrumbs do %>
  <li>
    <%= link_to "Dashboard", root_path, class: "font-medium text-slate-700 hover:text-slate-900" %>
  </li>
  <li class="text-slate-400">/</li>
  <li class="font-medium text-slate-900">Profile</li>
<% end %>

<div class="mx-auto max-w-2xl">
  <div class="flex flex-col gap-1">
    <div>
      <h1 class="text-2xl font-semibold tracking-tight text-slate-900">Your profile</h1>
      <p class="mt-1 text-sm text-slate-600">Update your personal details and password.</p>
    </div>
  </div>

  <div class="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">
    <%= form_with model: @user, url: profile_path, method: :patch, class: "divide-y divide-slate-200" do |f| %>
      <% if @user.errors.any? %>
        <div class="bg-rose-50 px-6 py-4">
          <p class="text-sm font-semibold text-rose-900">Please fix the following:</p>
          <ul class="mt-2 list-disc space-y-1 pl-5 text-sm text-rose-900">
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="px-6 py-5">
        <div class="flex items-center justify-between">
          <div>
            <h2 class="text-sm font-semibold text-slate-900">Contact</h2>
            <p class="mt-1 text-xs text-slate-500">How we reach you and how you sign in.</p>
          </div>
          <span class="inline-flex items-center rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-700">
            <%= @user.type %>
          </span>
        </div>

        <div class="mt-4 grid grid-cols-1 gap-4 sm:grid-cols-2">
          <div class="sm:col-span-1">
            <%= f.label :name, class: "block text-sm font-medium text-slate-700" %>
            <%= f.text_field :name,
                placeholder: "Full name",
                class: "mt-1 block w-full rounded-md border-slate-300 bg-white text-slate-900 shadow-sm focus:border-slate-900 focus:ring-slate-900" %>
          </div>

          <div class="sm:col-span-1">
            <%= f.label :email_address, "Email", class: "block text-sm font-medium text-slate-700" %>
            <%= f.email_field :email_address,
                autocomplete: "email",
                class: "mt-1 block w-full rounded-md border-slate-300 bg-white text-slate-900 shadow-sm focus:border-slate-900 focus:ring-slate-900" %>
          </div>

          <div class="sm:col-span-1">
            <%= f.label :phone, class: "block text-sm font-medium text-slate-700" %>
            <%= f.telephone_field :phone,
                autocomplete: "tel",
                class: "mt-1 block w-full rounded-md border-slate-300 bg-white text-slate-900 shadow-sm focus:border-slate-900 focus:ring-slate-900" %>
          </div>

          <div class="sm:col-span-2">
            <%= f.label :notes, class: "block text-sm font-medium text-slate-700" %>
            <%= f.text_area :notes, rows: 4,
                placeholder: "Optional notes…",
                class: "mt-1 block w-full rounded-md border-slate-300 bg-white text-slate-900 shadow-sm focus:border-slate-900 focus:ring-slate-900" %>
          </div>
        </div>
      </div>

      <div class="px-6 py-5">
        <h2 class="text-sm font-semibold text-slate-900">Password</h2>
        <p class="mt-1 text-xs text-slate-500">Leave blank to keep your current password.</p>

        <div class="mt-4 grid grid-cols-1 gap-4 sm:grid-cols-2">
          <div class="sm:col-span-1">
            <%= f.label :password, class: "block text-sm font-medium text-slate-700" %>
            <%= f.password_field :password,
                autocomplete: "new-password",
                class: "mt-1 block w-full rounded-md border-slate-300 bg-white text-slate-900 shadow-sm focus:border-slate-900 focus:ring-slate-900" %>
          </div>

          <div class="sm:col-span-1">
            <%= f.label :password_confirmation, class: "block text-sm font-medium text-slate-700" %>
            <%= f.password_field :password_confirmation,
                autocomplete: "new-password",
                class: "mt-1 block w-full rounded-md border-slate-300 bg-white text-slate-900 shadow-sm focus:border-slate-900 focus:ring-slate-900" %>
          </div>
        </div>
      </div>

      <div class="bg-white px-6 py-4">
        <div class="flex items-center justify-end">
          <%= f.submit "Save changes",
              class: "inline-flex items-center justify-center rounded-md bg-slate-900 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-slate-800 focus:outline-none focus:ring-2 focus:ring-slate-900 focus:ring-offset-2" %>
        </div>
      </div>
    <% end %>
  </div>
</div>


